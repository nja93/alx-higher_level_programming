-- Script that all records of the table second_table
-- Results both the score and the name (in this order)
-- Records ordered by score (top first)

SELECT score, name
FROM second_table ORDER BY score DESC; 
