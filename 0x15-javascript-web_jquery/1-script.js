$(document).ready(function(){
	// jQuery selector to find the <header> element and change its text colord
	// <header> element using $("header"), and then use the .css() method to change its text color using
	// color property
	$("header").css("color","#FF0000");
});
